{% import '_includes/forms' as forms %}

{{ forms.textField({
    first: true,
    instructions: 'seven API Key required for sending'|t('seven'),
    label: 'API Key'|t('seven'),
    name: 'apiKey',
    required: true,
    value: settings.apiKey
}) }}

{{ forms.textField({
    instructions: 'A custom sender identifier (caller ID)'|t('seven'),
    label: 'From'|t('seven'),
    name: 'from',
    value: settings.from
}) }}
